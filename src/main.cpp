// "Copyright 2025 Owner"
#include "Automata.h"

int main() {
    Automata automata;
    automata.on();
    automata.getMenu();

    automata.coin(200);
    automata.choice(1);
    automata.check();
    automata.cook();
    automata.finish();

    automata.off();
    return 0;
}
